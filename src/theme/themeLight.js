import { createTheme } from '@material-ui/core';
import palette from './paletteLight';
import typography from './typographyLight';

const themeLight = createTheme({
  palette,
  typography,
});

export default themeLight;
