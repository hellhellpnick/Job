import { createTheme } from '@material-ui/core';
import palette from './paletteDark';
import typography from './typographyDark';

const themeDark = createTheme({
  palette,
  typography,
});

export default themeDark;
