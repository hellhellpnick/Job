import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles(() => ({
  wrapper: {
    minHeight: '99vh',
    width: '100%',
    overflow: 'hidden'
  },
}));

export { useStyles };
